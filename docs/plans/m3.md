# soa-harness-impl — M3 Plan rev 2 (post-evaluator resolution)

**You are in `soa-harness-impl`.** Spec-session-authored plan rev 2 resolving 15 findings + 6 structural challenges from sibling-plan evaluator pass.

## Spec pin

| Field | Value |
|---|---|
| spec_commit_sha | `5e97277` — L-34 M3 sibling-plan evaluator resolution |
| Prior pins absorbed | `f05f950` (L-33 rev 2), `00c23c9` (L-33 kickoff), plus M1/M2 chain |
| spec_manifest_sha256 | `d211c3c94436fb43afc5c616284f35b6993d2fdfb9bcd45af09a8696aef3103e` |
| M3 test set | **139 tests** (was 145 — 4 SV-CLUS → M4, 2 SV-GOV → M5 per F-06/F-07) — target ≥120 green, **19-test skip budget** |
| Regression set | 22 M2 + 1 M1 retro (HR-12) must stay green |
| Deferred | M4 (SV-STR-12/13/14 + SV-SESS-12 + SV-CLUS-01..04), M5 (SV-SI 32 + SV-A2A 14 + HR-08/15/16/18 + SV-AGENTS-06 + SV-GOOD-01..07 + SV-GOV-10/12) |

## M3 test categories (refreshed count; totals verified from must-map)

| Category | Count |
|---|---|
| SV-MEM + SV-MEM-STATE | 8 + 2 = 10 |
| SV-BUD + SV-BUD-PROJ | 7 + 2 = 9 |
| SV-REG + SV-REG-OBS | 5 + 2 = 7 |
| SV-STR + SV-STR-OBS | 13 + 1 = 14 (3 transport → M4) |
| SV-HOOK | 8 |
| SV-CARD remainder | **11** (was 10; SV-CARD-01 is M3-tagged per must-map, not M1) |
| SV-PERM remainder | **21** (was ~15; full count per must-map) |
| SV-BOOT remainder | **6** (was 5) |
| SV-SIGN remainder | **5** (was 4) |
| SV-AGENTS remainder | 7 |
| SV-ENC | 7 |
| SV-PRIN | 5 |
| SV-STACK | 2 |
| SV-OPS | 2 |
| SV-GOV | **10** (was 12; -10/-12 retagged M5) |
| SV-PRIV | 5 |
| HR M3 | 10 |
| **TOTAL** | **139** |

## Pre-budgeted skips (subtract from green denominator)

Called out explicitly per S-5. These 4 tests are expected skips at M3 close regardless of impl completeness; "≥120 green of 139" translates to "≥120 of 135 non-pre-skip" with 15-test real slip budget.

| Test | Skip reason |
|---|---|
| SV-STR-04 | CrashEvent-last semantic plausibly needs §14.3 SSE terminal-event ordering beyond §14.5 polling — defer to validator judgment at exit |
| SV-GOV-09 | JSON-RPC -32061 version negotiation requires A2A (M5) |
| SV-MEM-08 | Cross-tenant memory isolation may need real Memory MCP (not mock) |
| HR-13 | ToolPoolStale on resume requires correlated registry_version + session.tool_pool_hash — if impl correlates via digests not semantic-version, observation gap |

## Week 1 — Memory + StreamEvent Foundation (+ Mock fixture)

### T-0 Ship Memory MCP mock fixture consumer `[NEW per F-08]`

- [ ] `tools/memory-mcp-mock/` subdirectory implementing the §8.1 three-tool protocol per spec fixture `test-vectors/memory-mcp-mock/README.md`
- [ ] Honor env vars: `SOA_MEMORY_MCP_MOCK_TIMEOUT_AFTER_N_CALLS`, `SOA_MEMORY_MCP_MOCK_RETURN_ERROR`, `SOA_MEMORY_MCP_MOCK_SEED`
- [ ] Load `test-vectors/memory-mcp-mock/corpus-seed.json` at startup when SEED env var is set
- [ ] Ship runnable binary invoked via `npm run mcp-mock` or equivalent
- [ ] 3 unit tests: happy-path round-trip, timeout-after-N behavior, error-injection

### T-1 Memory subsystem (§8) + GET /memory/state (§8.6)

- [ ] Memory MCP client per §8.1 (connect to `memory.mcp_endpoint` on the Agent Card)
- [ ] `search_memories()` / `write_memory()` / `consolidate_memories()`
- [ ] In-context selection per §8.2 (3-weight composite)
- [ ] Aging rules per Agent Card
- [ ] Consolidation trigger per §8.4
- [ ] Sharing-policy enforcement per §8.5
- [ ] `GET /memory/state` endpoint per §8.6 — schema `memory-state-response.schema.json`
- [ ] Tests: 10 (8 SV-MEM + SV-MEM-STATE-01/02)

### T-2 StreamEvent emission + GET /events/recent (§14.5)

- [ ] Emit all 25 §14.1 closed-enum event types at their firing boundaries. **The 25-type enum is explicitly enumerated in §14.1 lines 1889-1920** — use that list, not the impl plan rev 1 exemplars (which incorrectly named `SessionCreated` + `AuditSink*` + `CompactionDeferred` as enum members). Correct M3 scope:
  - **Session lifecycle:** `SessionStart`, `SessionEnd` (carries `stop_reason` — important for HR-17 observation per §8.3.1)
  - **Permission:** `PermissionPrompt`, `PermissionDecision`, `PermissionDenied`, `PermissionCapabilityDenied`
  - **Tool:** `ToolCallStart`, `ToolCallResult`, `ToolCallError`, `ToolPoolStaleResume`
  - **Content:** `MessageContent`, `ContentBlockDelta`, `ContentBlockStop`
  - **Budget:** `BudgetWarning` (emitted at compaction/budget boundary events)
  - **Resume/Crash:** `CrashEvent`, `ResumeCompensationGap`, `ResumeSucceeded`
  - **Audit:** `AuditSinkDegraded`, `AuditSinkUnreachable`, `AuditSinkRecovered` (surfaceable via §14.5, retained from M2)
  - **Observability:** `ObservabilityBackpressure`, `CardSignatureFailed`, `CardVersionDrift`, `HostHardeningInsufficient`
  - **SI/Handoff:** exactly 2 remaining enum slots for SelfImprovement* / Handoff* (emitted but handlers M5)
- [ ] Per-session monotonic `sequence`; unique `event_id`
- [ ] `GET /events/recent` — schema `events-recent-response.schema.json`, pagination
- [ ] Tests: 12 (SV-STR-01..11+15+16 minus transport deps, plus SV-STR-OBS-01)

### T-3 Scaffold Budget + Registry endpoint stubs

- [ ] `/budget/projection` + `/tools/registered` return schema-conformant placeholder bodies
- [ ] Enables validator to pre-wire schema validation in Week 1

**Week 1 exit:** Memory MCP mock running; 2 endpoints fully functional (/memory/state, /events/recent); 2 scaffolded; **22 M3 tests have impl coverage**.

## Week 2 — Budget + Registry + Hooks

### T-4 Budget subsystem (§13) + /budget/projection

- [ ] Projection per §13.1 (p95-over-W + 1.15 safety factor + cold-start baseline)
- [ ] Mid-stream enforcement per §13.2 (cancel at projected exhaustion; emit `BudgetWarning`/`SessionEnd{stop_reason:BudgetExhausted}`)
- [ ] Cache accounting per §13.3
- [ ] `GET /budget/projection` full per §13.5
- [ ] Tests: 11 (SV-BUD-01..07 + SV-BUD-PROJ-01/02 + HR-02/03/06)

### T-5 Dynamic MCP (§11.4) + /tools/registered

- [ ] Dynamic MCP client registration per §11.2 + runtime tool-add support
- [ ] Runtime tool-add test hook per §11.3.1 (NEW): `SOA_RUNNER_DYNAMIC_TOOL_REGISTRATION=<file>` env var — watch file; on write, invoke registration. Loopback-guard. Resolves F-09.
- [ ] Registry version = `sha256(JCS(tools[]))` per §11.4
- [ ] `registration_source ∈ {static-fixture, mcp-dynamic}`
- [ ] Tests: 9 (SV-REG-01..05 + SV-REG-OBS-01/02 + HR-13)

### T-6 Hooks pipeline §15

- [ ] PreToolUse + PostToolUse per §15
- [ ] Stdin/stdout JSON contract; exit-code routing; timeouts (5s pre / 10s post); fail-closed
- [ ] Wires into §10.3 step 5 dispatch
- [ ] Tests: 8 (SV-HOOK-01..08)

**Week 2 exit:** 4 endpoints fully functional. Hooks live. Cumulative impl coverage: **50 M3 tests**.

## Week 3 — Core-Profile Completion + Week-4 Scaffolds

Low-risk, high-volume. Includes Week-4 scaffold pattern extension per S-3/F-14.

### T-7 SV-CARD/SV-PERM/SV-BOOT/SV-SIGN remainders — 43 tests

- [ ] SV-CARD-02..12 (11 tests): field-level assertions
- [ ] SV-PERM-02..22 (21 tests): three-axis depth, precedence, escalation, audit linkage, key rotation
- [ ] SV-BOOT-02..07 (6 tests): bootstrap variants
- [ ] SV-SIGN-02..06 (5 tests): alg/typ/x5c depth

### T-8 Policy/manifest block — 16 tests

- [ ] SV-ENC-01..07 (7)
- [ ] SV-PRIN-01..05 (5)
- [ ] SV-STACK-01..02 (2)
- [ ] SV-OPS-01..02 (2)

### T-9 SV-AGENTS remainder — 7 tests

- [ ] SV-AGENTS-01..05, 07, 08

### T-10 Testable-today HRs — 6 tests

- [ ] HR-01, HR-07, HR-09, HR-10, HR-11, HR-13

### T-11 §19.4 version-policy errata `[time-boxed 1 day per F-13]`

- [ ] Single day at most; place mid-Week-3 after T-8
- [ ] `packages/runner/package.json` + `release-manifest.json` carry v1.0 version + errata-track notation
- [ ] Repo README note: "consuming v1.0 includes all §X.Y Observability additions per spec MANIFEST supplementary_artifacts"

### T-11b Week-4 endpoint scaffolds `[NEW per S-3/F-14]`

- [ ] Ship placeholder bodies for governance/privacy surfaces referenced by Week-4 T-12 handlers:
  - `/data-inventory` endpoint placeholder OR publish the required `docs/data-inventory.md` file
  - `privacy.delete_subject` + `privacy.export_subject` MCP tools stubbed so validator can invoke them
- [ ] Schema-conformant placeholder bodies only; full behavior in T-12 next week
- [ ] Enables validator V-13 to pre-wire handlers in Week 3 alongside T-7/T-8 instead of serial-wait for Week 4

**Week 3 exit:** **122 M3 tests** impl-covered (cumulative). Week-4 scaffolds ready for validator pre-wire.

## Week 4 — Governance, HRs, Integration

### T-12 Governance block — SV-GOV (10) + SV-PRIV (5) = 15 tests

- [ ] SV-GOV-01..08, 11 (removing the M5-retagged -10/-12): runtime card fetch for `soaHarnessVersion`, errata URL reachability, version-negotiation errors, etc.
- [ ] SV-PRIV-01..05: §10.7 privacy (data-inventory presence, field-level tagging, subject-ops, residency, retention)
- [ ] Validator pre-wired these in Week 3 via T-11b scaffolds; this week just fills real behavior

### T-13 Endpoint-dependent HRs — 4 tests

- [ ] HR-02: Projection over budget → `SessionEnd{stop_reason:BudgetExhausted}` via `/events/recent` (per F-01 clarification)
- [ ] HR-03: Mid-stream cancel — observe `SessionEnd{stop_reason:BudgetExhausted}` emission during active decision
- [ ] HR-06: Budget depleted between turns — projection + cache-accounting correlation
- [ ] HR-17: 3 Memory MCP timeouts — drive via `SOA_MEMORY_MCP_MOCK_TIMEOUT_AFTER_N_CALLS=0`; observe `SessionEnd{stop_reason:MemoryDegraded}` per §8.3.1

### T-14 Integration + exit gate

- [ ] Full 139 M3 + 23 regression suite green on Linux/macOS/Windows
- [ ] Skip count ≤ 19 (else raise before close)
- [ ] `release-gate.json` v2 per-platform evidence for all 162 IDs
- [ ] Cold-cache timings recorded
- [ ] `STATUS.md` final entry

## Exit gate

- [ ] ≥120 M3 + 22 M2 + 1 M1 retro = **≥143 total** across 3 platforms
- [ ] Skip count ≤ 19 (subtracting the 4 pre-budgeted leaves ≤15 real slips)
- [ ] Zero fails, zero errors

## STATUS.md schema (per S-4)

End-of-day signal in a machine-readable block for validator consumption:

```
## <date>
- Done: <narrative>
- Active: <narrative>
- Blocked: <narrative>
- Pin: <spec_commit_sha>
- Scoreboard: <narrative>

<!-- machine-readable -->
{
  "week": 2,
  "t_tasks_landed": ["T-4", "T-5"],
  "endpoints_live": ["/memory/state", "/events/recent", "/budget/projection", "/tools/registered"],
  "memory_mcp_mock_endpoint": "http://127.0.0.1:8001",
  "spec_pin": "5e97277"
}
<!-- /machine-readable -->
```

Validator parses the machine-readable block to auto-flip handlers without natural-language latency.

## Env vars introduced this milestone (delta from M2)

| Variable | Source |
|---|---|
| `SOA_RUNNER_DYNAMIC_TOOL_REGISTRATION=<file>` | §11.3.1 (L-34) |
| `SOA_MEMORY_MCP_MOCK_TIMEOUT_AFTER_N_CALLS=<n>` | memory-mcp-mock fixture |
| `SOA_MEMORY_MCP_MOCK_RETURN_ERROR=<tool_name>` | memory-mcp-mock fixture |
| `SOA_MEMORY_MCP_MOCK_SEED=<path>` | memory-mcp-mock fixture |

## Schemas consumed

Inherits all M1/M2 schemas. M3 adds:
- `memory-state-response.schema.json`
- `budget-projection-response.schema.json`
- `tools-registered-response.schema.json`
- `events-recent-response.schema.json`

## Fixtures consumed

Inherits M1/M2. M3 adds:
- `test-vectors/memory-mcp-mock/README.md`
- `test-vectors/memory-mcp-mock/corpus-seed.json`

## Plan refresh triggers

Spec-session rewrites this file when §8, §8.3, §8.3.1, §8.6, §11, §11.3.1, §11.4, §13, §13.5, §14, §14.5, §15, or any of the M3 schemas/fixtures change.

## Daily status protocol

Unchanged from M2 rev 2 + the STATUS.md machine-readable block above.
